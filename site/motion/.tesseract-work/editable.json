{
  "$schema": "https://jerboa.dev/schemas/fx-composition/editable/v1/document.schema.json",
  "formatVersion": 1,
  "dimensions": {
    "width": 1920,
    "height": 1080
  },
  "duration": 12,
  "composition": {
    "id": "main",
    "layers": [
      {
        "type": "Image",
        "id": 2,
        "name": "Janitor sweep mark",
        "blendMode": "normal",
        "activeRange": {
          "start": 0,
          "duration": 12000
        },
        "transform": {
          "anchorPoint": [
            627,
            627
          ],
          "position": [
            1325,
            518
          ],
          "scale": [
            24,
            24
          ],
          "rotation": 0,
          "opacity": 100
        },
        "source": {
          "assetId": "janitor-mark",
          "fit": "contain"
        },
        "effects": [
          {
            "id": 2,
            "effect": {
              "type": "customShader",
              "name": "darkMatteComposite",
              "description": "Composite the generated opaque logo onto the signal field using its luminance; preserve antialiased mint edges.",
              "wgsl": "// Original Janitor continuity field. Bounded, periodic, no textures or randomness.\nstruct VertexInput {\n  @location(0) position: vec2<f32>,\n  @location(1) tex_coords: vec2<f32>,\n  @location(2) color: vec4<f32>,\n};\nstruct VertexOutput {\n  @builtin(position) clip_position: vec4<f32>,\n  @location(0) tex_coords: vec2<f32>,\n};\n@vertex fn vs_main(input: VertexInput) -> VertexOutput {\n  var out: VertexOutput;\n  out.clip_position = vec4<f32>(input.position, 0.0, 1.0);\n  out.tex_coords = input.tex_coords;\n  return out;\n}\n@group(0) @binding(0) var t_texture: texture_2d<f32>;\n@group(0) @binding(1) var s_sampler: sampler;\n@fragment fn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\n  let c=textureSample(t_texture,s_sampler,input.tex_coords);\n  let alpha=smoothstep(0.08,0.25,max(c.r,max(c.g,c.b)))*c.a;\n  return vec4<f32>(c.rgb*alpha,alpha);\n}\n",
              "params": []
            }
          }
        ]
      },
      {
        "type": "Rect",
        "id": 1,
        "name": "Periodic continuity field",
        "blendMode": "normal",
        "activeRange": {
          "start": 0,
          "duration": 12000
        },
        "transform": {
          "anchorPoint": [
            0,
            0
          ],
          "position": [
            0,
            0
          ],
          "scale": [
            100,
            100
          ],
          "rotation": 0,
          "opacity": 100
        },
        "rect": {
          "size": [
            1920,
            1080
          ],
          "fillColor": [
            0.03137,
            0.04706,
            0.04706,
            1
          ]
        },
        "effects": [
          {
            "id": 1,
            "effect": {
              "type": "customShader",
              "name": "janitorContinuityField",
              "description": "48 fine historical traces, three continuity orbits and one scanning light. A twelve-second closed loop; decorative, not live user data.",
              "wgsl": "// Original Janitor continuity field. Bounded, periodic, no textures or randomness.\nstruct VertexInput {\n  @location(0) position: vec2<f32>,\n  @location(1) tex_coords: vec2<f32>,\n  @location(2) color: vec4<f32>,\n};\nstruct VertexOutput {\n  @builtin(position) clip_position: vec4<f32>,\n  @location(0) tex_coords: vec2<f32>,\n};\n@vertex fn vs_main(input: VertexInput) -> VertexOutput {\n  var out: VertexOutput;\n  out.clip_position = vec4<f32>(input.position, 0.0, 1.0);\n  out.tex_coords = input.tex_coords;\n  return out;\n}\nstruct Params { animationTime: f32, period: f32, intensity: f32, _pad: f32 };\n@group(0) @binding(0) var t_texture: texture_2d<f32>;\n@group(0) @binding(1) var s_sampler: sampler;\n@group(0) @binding(2) var<uniform> params: Params;\n\nfn rotate(p: vec2<f32>, a: f32) -> vec2<f32> {\n  return vec2<f32>(cos(a)*p.x - sin(a)*p.y, sin(a)*p.x + cos(a)*p.y);\n}\nfn line(d: f32, width: f32) -> f32 {\n  return 1.0 - smoothstep(width, width + 0.0018, abs(d));\n}\nfn field(uv: vec2<f32>) -> vec3<f32> {\n  let t = params.animationTime / params.period * 6.2831853;\n  let p = (uv - vec2<f32>(0.69,0.48)) * vec2<f32>(3.55556,2.0);\n  let mint = vec3<f32>(0.714,0.961,0.808);\n  var col = vec3<f32>(0.03137,0.04706,0.04706);\n  // A faint, local pool of light: black margins remain clear for HTML copy.\n  col += vec3<f32>(0.011,0.025,0.018) * exp(-3.0*dot(p,p));\n  let q = rotate(p, -0.34);\n  let tilt = 0.76 + 0.035*sin(t);\n  let radial = length(vec2<f32>(q.x, q.y/tilt));\n  let angle = atan2(q.y/tilt, q.x);\n  let scan = pow(0.5 + 0.5*cos(angle-t), 24.0);\n  // Fine concentric records. Their spacing and phase form a softly turning rim.\n  for(var i=0; i<48; i=i+1) {\n    let f = f32(i)/47.0;\n    let radius = 0.39 + f*0.46;\n    let wave = 0.017*sin(angle*3.0 + t) * sin(f*3.14159265);\n    let edge = line(radial-radius-wave, 0.0007);\n    let rim = 0.07 + 0.16*pow(0.5+0.5*cos(angle+1.2),6.0) + 0.38*scan;\n    let contour = 0.45 + 0.55*sin(f*3.14159265);\n    col += mint*edge*rim*contour*params.intensity;\n  }\n  // Three independently preserved traces run around the same stable environment.\n  for(var j=0; j<3; j=j+1) {\n    let f=f32(j);\n    let e=rotate(p, -0.34 + f*0.032);\n    let r=length(vec2<f32>(e.x, e.y/(0.76+0.035*sin(t))));\n    let a=atan2(e.y/tilt,e.x);\n    let trace=line(r-(0.9+f*0.055),0.0010);\n    let pulse=pow(0.5+0.5*cos(a-t-f*1.7),100.0);\n    col += mint*trace*(0.075+pulse*0.7);\n  }\n  // Quiet instrument ticks, fixed rather than particle noise.\n  let tick = pow(abs(cos(angle*72.0)), 30.0);\n  col += mint*tick*(1.0-smoothstep(0.014,0.017,abs(radial-1.09)))*0.18;\n  let centerShade = smoothstep(0.18,0.36,length(p));\n  return mix(vec3<f32>(0.03137,0.04706,0.04706),col,centerShade);\n}\n@fragment fn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\n  return vec4<f32>(clamp(field(input.tex_coords),vec3<f32>(0.0),vec3<f32>(1.0)),1.0);\n}\n",
              "params": [
                {
                  "name": "animationTime",
                  "description": "Engine-driven local time, seconds",
                  "min": 0,
                  "max": 12,
                  "default": 0
                },
                {
                  "name": "period",
                  "description": "Complete loop duration, seconds",
                  "min": 1,
                  "max": 60,
                  "default": 12
                },
                {
                  "name": "intensity",
                  "description": "Rim light intensity",
                  "min": 0,
                  "max": 2,
                  "default": 1
                }
              ]
            }
          }
        ]
      }
    ],
    "name": "Janitor — continuity field"
  }
}
