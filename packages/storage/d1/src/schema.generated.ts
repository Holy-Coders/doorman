// Generated by scripts/schema-bundle.mjs. Do not edit.
export const SCHEMA_VERSION = 10;
export const SCHEMA_STATEMENTS: readonly string[] = [
  "CREATE TABLE IF NOT EXISTS visitors (\n  id TEXT PRIMARY KEY,\n  created_at INTEGER NOT NULL,\n  last_seen_at INTEGER NOT NULL\n)",
  "CREATE TABLE IF NOT EXISTS observations (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  visitor_id TEXT NOT NULL REFERENCES visitors(id) ON DELETE CASCADE,\n  seen_at INTEGER NOT NULL,\n  platform TEXT,\n  browser TEXT,\n  timezone TEXT,\n  webgl_renderer TEXT,\n  signals_json TEXT NOT NULL\n)",
  "CREATE INDEX IF NOT EXISTS idx_observation_platform_browser ON observations(platform, browser, seen_at DESC)",
  "CREATE INDEX IF NOT EXISTS idx_observation_webgl_renderer ON observations(webgl_renderer, seen_at DESC)",
  "CREATE INDEX IF NOT EXISTS idx_observation_timezone_browser ON observations(timezone, browser, seen_at DESC)",
  "CREATE INDEX IF NOT EXISTS idx_observation_visitor_seen ON observations(visitor_id, seen_at DESC, id DESC)",
  "CREATE INDEX IF NOT EXISTS idx_observation_seen ON observations(seen_at)",
  "CREATE INDEX IF NOT EXISTS idx_visitor_seen ON visitors(last_seen_at)",
  "CREATE TABLE IF NOT EXISTS identity_subjects (\n  id TEXT PRIMARY KEY,\n  kind TEXT NOT NULL CHECK (kind IN ('person', 'agent')),\n  record TEXT NOT NULL\n)",
  "CREATE TABLE IF NOT EXISTS identity_keys (\n  digest TEXT PRIMARY KEY,\n  subject_id TEXT NOT NULL REFERENCES identity_subjects(id) ON DELETE CASCADE,\n  record TEXT NOT NULL\n)",
  "CREATE INDEX IF NOT EXISTS idx_identity_keys_subject ON identity_keys(subject_id)",
  "CREATE TABLE IF NOT EXISTS identity_delegations (\n  id TEXT PRIMARY KEY,\n  principal_id TEXT NOT NULL REFERENCES identity_subjects(id) ON DELETE CASCADE,\n  actor_id TEXT NOT NULL REFERENCES identity_subjects(id) ON DELETE CASCADE,\n  expires_at BIGINT NOT NULL,\n  record TEXT NOT NULL\n)",
  "CREATE INDEX IF NOT EXISTS idx_identity_delegations_expiry ON identity_delegations(expires_at)",
  "CREATE INDEX IF NOT EXISTS idx_identity_delegations_principal ON identity_delegations(principal_id)",
  "CREATE INDEX IF NOT EXISTS idx_identity_delegations_actor ON identity_delegations(actor_id)",
  "CREATE TABLE IF NOT EXISTS learning_sessions (\n  id TEXT PRIMARY KEY,\n  scope TEXT NOT NULL,\n  started_at BIGINT NOT NULL,\n  expires_at BIGINT NOT NULL,\n  observed_at BIGINT NOT NULL,\n  signals_json TEXT NOT NULL,\n  subject_id TEXT REFERENCES identity_subjects(id) ON DELETE CASCADE,\n  verified_at BIGINT,\n  disputed INTEGER NOT NULL DEFAULT 0,\n  prediction_status TEXT NOT NULL,\n  predicted_subject_id TEXT REFERENCES identity_subjects(id) ON DELETE CASCADE,\n  prediction_score REAL\n)",
  "CREATE INDEX IF NOT EXISTS idx_learning_scope_verified ON learning_sessions(scope, verified_at DESC)",
  "CREATE INDEX IF NOT EXISTS idx_learning_scope_expiry ON learning_sessions(scope, expires_at)",
  "CREATE INDEX IF NOT EXISTS idx_learning_subject_verified ON learning_sessions(subject_id, verified_at DESC)",
  "CREATE INDEX IF NOT EXISTS idx_learning_predicted_subject ON learning_sessions(predicted_subject_id)",
  "CREATE INDEX IF NOT EXISTS idx_observation_screen_hardware ON observations(platform, browser, json_extract(signals_json, '$.screen.width'), json_extract(signals_json, '$.screen.height'), json_extract(signals_json, '$.hardware.hardwareConcurrency'), seen_at DESC)",
  "CREATE INDEX IF NOT EXISTS idx_observation_screen_timezone ON observations(platform, browser, json_extract(signals_json, '$.screen.width'), json_extract(signals_json, '$.screen.height'), timezone, seen_at DESC)",
  "CREATE INDEX IF NOT EXISTS idx_observation_graphics_timezone ON observations(platform, browser, webgl_renderer, timezone, seen_at DESC)",
  "CREATE TABLE IF NOT EXISTS protection_quotas (\n  id TEXT PRIMARY KEY,\n  used INTEGER NOT NULL,\n  reset_at INTEGER NOT NULL\n)",
  "CREATE INDEX IF NOT EXISTS idx_protection_quota_expiry ON protection_quotas(reset_at)",
  "CREATE TABLE IF NOT EXISTS evaluation_controls (\n  id TEXT PRIMARY KEY,\n  version INTEGER NOT NULL,\n  expires_at INTEGER NOT NULL,\n  record TEXT NOT NULL\n)",
  "CREATE INDEX IF NOT EXISTS idx_evaluation_control_expiry ON evaluation_controls(expires_at)",
  "CREATE TABLE IF NOT EXISTS application_events (\n  id TEXT PRIMARY KEY,\n  scope TEXT NOT NULL,\n  digest TEXT NOT NULL,\n  subject_id TEXT REFERENCES identity_subjects(id) ON DELETE CASCADE,\n  session_id TEXT,\n  actor_id TEXT REFERENCES identity_subjects(id) ON DELETE CASCADE,\n  visitor_id TEXT REFERENCES visitors(id) ON DELETE CASCADE,\n  action TEXT,\n  occurred_at INTEGER NOT NULL,\n  expires_at INTEGER NOT NULL,\n  record TEXT NOT NULL\n)",
  "CREATE INDEX IF NOT EXISTS idx_events_subject_time ON application_events(subject_id,scope,occurred_at DESC,id DESC)",
  "CREATE INDEX IF NOT EXISTS idx_events_session_time ON application_events(session_id,scope,occurred_at DESC,id DESC)",
  "CREATE INDEX IF NOT EXISTS idx_events_actor_time ON application_events(actor_id,scope,occurred_at DESC,id DESC)",
  "CREATE INDEX IF NOT EXISTS idx_events_subject_action ON application_events(subject_id,scope,action,occurred_at DESC,id DESC)",
  "CREATE INDEX IF NOT EXISTS idx_events_session_action ON application_events(session_id,scope,action,occurred_at DESC,id DESC)",
  "CREATE INDEX IF NOT EXISTS idx_events_actor_action ON application_events(actor_id,scope,action,occurred_at DESC,id DESC)",
  "CREATE INDEX IF NOT EXISTS idx_events_expiry ON application_events(scope,expires_at)",
  "CREATE INDEX IF NOT EXISTS idx_events_visitor ON application_events(visitor_id)",
  "CREATE TABLE IF NOT EXISTS device_links (\n  id TEXT PRIMARY KEY,\n  scope TEXT NOT NULL,\n  digest TEXT NOT NULL,\n  subject_id TEXT NOT NULL REFERENCES identity_subjects(id) ON DELETE CASCADE,\n  visitor_id TEXT NOT NULL REFERENCES visitors(id) ON DELETE CASCADE,\n  created_at INTEGER NOT NULL,\n  retire_at INTEGER NOT NULL,\n  record TEXT NOT NULL\n)",
  "CREATE INDEX IF NOT EXISTS idx_device_links_subject ON device_links(subject_id,scope,created_at DESC,id DESC)",
  "CREATE INDEX IF NOT EXISTS idx_device_links_visitor ON device_links(visitor_id)",
  "CREATE INDEX IF NOT EXISTS idx_device_links_retire ON device_links(scope,retire_at)",
  "CREATE INDEX IF NOT EXISTS idx_learning_scope_language ON learning_sessions (scope, json_extract(signals_json, '$.languages[0]'), verified_at DESC, id DESC) WHERE subject_id IS NOT NULL AND disputed = 0",
  "CREATE INDEX IF NOT EXISTS idx_learning_scope_timezone ON learning_sessions (scope, json_extract(signals_json, '$.timezone'), verified_at DESC, id DESC) WHERE subject_id IS NOT NULL AND disputed = 0",
  "CREATE TABLE IF NOT EXISTS api_activity_buckets (\n  owner TEXT NOT NULL,\n  window_start INTEGER NOT NULL,\n  route TEXT NOT NULL,\n  requests INTEGER NOT NULL,\n  denied INTEGER NOT NULL,\n  client_errors INTEGER NOT NULL,\n  server_errors INTEGER NOT NULL,\n  duration_total_ms INTEGER NOT NULL,\n  duration_max_ms INTEGER NOT NULL,\n  first_seen_at INTEGER NOT NULL,\n  last_seen_at INTEGER NOT NULL,\n  short_gaps INTEGER NOT NULL,\n  expires_at INTEGER NOT NULL,\n  PRIMARY KEY (owner, window_start, route)\n)",
  "CREATE INDEX IF NOT EXISTS idx_api_activity_expiry ON api_activity_buckets(expires_at)",
  "CREATE TABLE IF NOT EXISTS api_activity_assessments (\n  id TEXT PRIMARY KEY,\n  owner TEXT NOT NULL,\n  lease TEXT NOT NULL,\n  next_at INTEGER NOT NULL,\n  record TEXT\n)",
  "CREATE INDEX IF NOT EXISTS idx_api_assessment_owner ON api_activity_assessments(owner)",
  "CREATE INDEX IF NOT EXISTS idx_api_assessment_expiry ON api_activity_assessments(next_at)",
  "CREATE TABLE IF NOT EXISTS operator_windows (\n  account_key TEXT NOT NULL,\n  id TEXT NOT NULL,\n  browser_key TEXT,\n  session_key TEXT NOT NULL,\n  started_at INTEGER NOT NULL,\n  ended_at INTEGER NOT NULL,\n  expires_at INTEGER NOT NULL,\n  status TEXT NOT NULL,\n  lease TEXT NOT NULL,\n  record TEXT NOT NULL,\n  PRIMARY KEY (account_key, id)\n)",
  "CREATE INDEX IF NOT EXISTS idx_operator_account_seen ON operator_windows(account_key, started_at DESC, id)",
  "CREATE INDEX IF NOT EXISTS idx_operator_browser ON operator_windows(account_key, browser_key)",
  "CREATE INDEX IF NOT EXISTS idx_operator_session ON operator_windows(account_key, session_key)",
  "CREATE INDEX IF NOT EXISTS idx_operator_expiry ON operator_windows(expires_at)",
  "CREATE TABLE IF NOT EXISTS browser_associations (\n  id TEXT PRIMARY KEY,\n  scope TEXT NOT NULL,\n  visitor_id TEXT NOT NULL REFERENCES visitors(id) ON DELETE CASCADE,\n  subject_id TEXT NOT NULL REFERENCES identity_subjects(id) ON DELETE CASCADE,\n  actor_id TEXT NOT NULL REFERENCES identity_subjects(id) ON DELETE CASCADE,\n  seen_at INTEGER NOT NULL,\n  expires_at INTEGER NOT NULL,\n  record TEXT NOT NULL\n)",
  "CREATE INDEX IF NOT EXISTS idx_association_browser ON browser_associations(visitor_id,scope,expires_at DESC,id)",
  "CREATE INDEX IF NOT EXISTS idx_association_subject ON browser_associations(subject_id)",
  "CREATE INDEX IF NOT EXISTS idx_association_expiry ON browser_associations(scope,expires_at)",
  "CREATE INDEX IF NOT EXISTS idx_association_actor ON browser_associations(actor_id)",
];
