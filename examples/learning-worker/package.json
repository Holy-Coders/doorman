{
  "name": "@janitor/example-learning-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "typecheck": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply NETWORK --local",
    "migrate:remote": "wrangler d1 migrations apply NETWORK --remote",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@janitor/network": "workspace:^"
  },
  "devDependencies": {
    "wrangler": "^4.136.3"
  }
}
