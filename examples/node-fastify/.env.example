DATABASE_URL=postgres://visitor:visitor@localhost:5432/visitors
# Leave blank to run deterministic matching without external AI calls.
JEV_API_KEY=
# Optional API activity demo; set both secrets to distinct random values before enabling.
JANITOR_API_ACTIVITY=0
JANITOR_IDENTITY_SECRET=
EXAMPLE_API_TOKEN=
